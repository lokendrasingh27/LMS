/*
const lecture = require('path lecture')

const getAllVideos = async(req,res) =>{
    try{
        const course= await course.findById(request.params.courseId).onfail()
        const videos = await course.getVideos();
        return res.json(videos)
    }
    catch(err){
        console.log(err);
        res.status(4000).json({ error: err.message || err.toString()})
    }
}
export default {
    getAllVideos,
} 
*/